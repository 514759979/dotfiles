#!/bin/bash
if [ $# = 0 ]; then
    str='systemctl halt'
elif [ $1 = 1 ]; then
    str='systemctl reboot'
elif [ $1 = 2 ]; then
    str="kexec /boot/vmlinuz-linux --append='root=/dev/sda2 ro' --initrd=/boot/initramfs-linux.img"
fi
sudo $str
